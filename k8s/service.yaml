apiVersion: v1
kind: Service
metadata:
  name: web-check
  labels:
    app: web-check
spec:
  type: NodePort
  selector:
    app: web-check
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30080
